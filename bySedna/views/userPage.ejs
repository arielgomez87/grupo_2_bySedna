<!DOCTYPE html>
<html lang="es">

<head>
    <%- include ("./partials/head", {title: 'Login a ' }) %>
</head>

<body>
    <header class="main-header">
        <%- include ("./partials/header") %>
    </header>

    <main class="main-login">
        <% console.log(user) %>
        <div class="form-login">
            <h3>Hola <%= user.fullName %> Aca podes encontrar toda tu informacion de usuario.<h3>
        </div>
        <div class="form-login">
            <img src="/images/users/<%=user.imageUser%> " width="150" height="180">
        </div>
        <div class="form-login">
            <h5>Tu email es: <%=user.email %>
            </h5>
            <br>
            <h6>Tu direccion de envio es: <%=user.address %>
            </h6>
            <br>
            <div class="btn-userPage">
                <a href="/users/edit/<%=user.id%>"><button class="btn btn-success" type="button">Editar</button></a>
                <a href="/users/logout"><button class="btn btn-danger" type="button">Logout</button></a>
                <div>
                    <form action="/users/delete/<%=user.id%>" method="POST">
                        <button type="submit" class="btn btn-outline-danger">Eliminar usuario</button>
                    </form>
                </div>
            </div>
        </div>

    </main>
    <footer>
        <%- include ("./partials/footer") %>
    </footer>

</body>

</html>